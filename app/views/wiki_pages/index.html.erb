<div class="ui container">
  <div class="ui large header">위키:대문</div>
  <div class="ui divider"></div>

  <p>SNUCMS+ 위키에 오신 것을 환영합니다!</p>

  <p>모든 문서 목록</p>
  <div class="ui bulleted list">
    <% @pages.each do |page| %>
      <div class="item"><%= link_to page.title, wiki_page_path(page.title) %></div>
    <% end %>
  </div>

  <div class="ui divider"></div>
  <div class="ui button"><%= link_to 'Back', root_path %></div>
</div>
